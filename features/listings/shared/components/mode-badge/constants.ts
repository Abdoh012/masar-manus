// Badge-scoped static data only. The mode option list itself lives in
// features/listings/shared/lib/constants.ts (R-1) — this file holds the
// pill styling and a11y copy specific to the ModeBadge leaf.

export const MODE_BADGE_ARIA_LABEL = "Training mode";

export const MODE_BADGE_CLASSES =
  "rounded-full bg-secondary-tint px-2.5 py-1 text-[11px] font-semibold uppercase tracking-wide text-secondary-text";